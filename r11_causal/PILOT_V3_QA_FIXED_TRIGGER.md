# Pilot v3 QA-fixed trigger

All scientific calculations are unchanged from the v3 design. The only prior failure was a stale summary dictionary key after computation. This marker triggers the corrected development run.
