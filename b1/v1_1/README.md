# POLAR DYNAMICS — B1-D v1.1

This prospective Program A iteration addresses the four blockers frozen in B1-D v1. The historical code, observations, seed exclusions, B0 contracts and previous manifests remain unchanged. Program B stays B-design.

The executable evidence has separate purposes:

- `p5/`: a newly frozen development comparison, using minimal-next-demand C4 and a separately named full-refill ceiling witness. P5 remains ineligible for positive utility and pairing superiority.
- `resources/`: finite task/controller bounds, actual-use profiling and QA replay. Larger common allowances preserve selected P7 actions. The certificate concerns declared logical opportunity, with explicit limitations for physical CPU, allocation and latency.
- `exporter/`: authenticated archive replay and a fail-closed future pipeline. Its recovered 96 diagnostics are historical observations, never a new sample. The original incomplete 42-row summary and undetermined incident remain preserved.
- `statistics/`: prospective fixed-N paired-mean inference and synthetic validation. Simulations test the statistical method, not Polar Dynamics.
- `retention/`: a predeclared audit subset and complete causal evidence, with full traces retained for failures and violations. Resource projections are estimates, not reservations.

Every scientific development result sets `development_only=true`, `confirmatory=false`, and `reusable_as_final=false`. New task seeds use `PD-B1-D-v1.1`; engineering QA uses `PD-B1-D-v1.1-QA`. Replays explicitly retain their original identity. No final seeds or B1-E execution are implemented here.

The selected P7 C1 policy remains reactive `cfg01`. Historical paired and acute route differences remain zero descriptively. Source/sham manipulations remain joint and cannot establish Gamma-specific selectivity. Negative controls and null findings are retained without favorable reinterpretation.

Run the original tests with `python -B -m unittest discover -s b1/tests -v` and new tests with `python -B -m unittest discover -s b1/v1_1 -t . -p 'test_*.py' -v`. The old recovery validator expects exactly the old Python inventory and is tested in a detached checkout of the pinned v1 commit. The new validator checks both the immutable base and the additive v1.1 inventory.

Read `B1D_V1_1_RESULTS.json`, `BLOCKER_CLOSURE_MATRIX.csv` and the final `B1D_V1_1_MANIFEST.json` together. Completion means the development instrument is technically ready under its stated scope; it does not imply empirical superiority, generalization, consciousness, AGI or ASI. Final confirmatory authorization always remains false in this iteration.
