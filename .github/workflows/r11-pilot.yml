name: POLAR R11a pilot archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "R11a development pilot is archived; see R11A_PILOT_ADJUDICATION.md."
